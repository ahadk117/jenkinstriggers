pipeline {
    agent any

    stages {   // ✅ All stage blocks go inside here
        stage('Building a stage') {
            steps {
                sh 'echo building a build'
            }
        }
    }
}
